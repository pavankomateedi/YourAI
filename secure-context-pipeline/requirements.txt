# Core runtime + test dependencies that install cleanly on a local venv,
# including very recent Python versions (no spaCy/Presidio compiled deps here).
# For the full NER stack, use the Docker image or `pip install -e .[nlp]`.
cryptography>=42.0
faker>=24.0
python-dotenv>=1.0
httpx>=0.27
anthropic>=0.39
pypdf>=4.0
python-docx>=1.1
pytest>=7.4
pytest-asyncio>=0.21
pytest-benchmark>=4.0
pytest-mock>=3.11
