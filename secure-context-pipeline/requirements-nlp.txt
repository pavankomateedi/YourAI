# Full NER detection stack (Presidio + spaCy). Use inside Docker / Python 3.11.
# After installing, download the spaCy model:
#     python -m spacy download en_core_web_lg
-r requirements.txt
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7
